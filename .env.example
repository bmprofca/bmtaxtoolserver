# MySQL database (Hostinger)
# Remote hostname for LOCAL development only: srv1946.hstgr.io
# LIVE server (nodejs on Hostinger): always DB_HOST=localhost (deploy script sets this)
# For local dev: hPanel → Databases → Remote MySQL → add your public IP
DB_HOST=srv1946.hstgr.io
DB_PORT=3306
DB_NAME=u278432002_tool
DB_USER=u278432002_tool
DB_PASSWORD=your_password_here
# Max pooled connections (keep low on shared hosting; default 3)
DB_POOL_LIMIT=3

# Server
PORT=3001
